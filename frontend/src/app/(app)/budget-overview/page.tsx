import { WalletCards } from 'lucide-react';
import { ModulePlaceholder } from '@/components/module-placeholder';

export default function BudgetOverviewPage() {
  return <ModulePlaceholder title="Budget Overview" description="Track planned budgets, category allocations, real spending, and remaining runway across every trip." icon={WalletCards} />;
}
