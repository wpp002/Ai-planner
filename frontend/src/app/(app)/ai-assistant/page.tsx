import { Bot } from 'lucide-react';
import { ModulePlaceholder } from '@/components/module-placeholder';

export default function AiAssistantPage() {
  return <ModulePlaceholder title="AI Assistant" description="Ask for trip alternatives, cheaper routes, rainy-day plans, or local recommendations." icon={Bot} />;
}
